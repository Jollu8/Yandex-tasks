#include <iostream>
#include <vector>
#include <string_view>
#include <string>
#include <sstream>
#include <algorithm>
#include <cmath>

using namespace std;

int minimumOperations(vector<int>& nums) {
    int size = 0;
    int res = 0;
    while(size <= nums.size()) {
        for(auto &i:nums) {
            if()
        }
    }

}

int main() {
    cout << numberOfSteps(123);
}